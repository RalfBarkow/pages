{
  "title": "Derive Type of Chosen Variable",
  "story": [
    {
      "type": "markdown",
      "id": "8b4d6c1334d7a5e7",
      "text": "Based on the [[recommend]] function, we can deduce the type of the `chosen` variable: (ChatGPT 3.5)"
    },
    {
      "type": "code",
      "id": "12165aba1e6bfd6d",
      "text": "for (const poem of chosen) {\n    for (const node of poem.graph.nodes) {\n        vocabulary.add(hash(node));\n    }\n}"
    },
    {
      "type": "markdown",
      "id": "83e7a6b3bcc07e51",
      "text": "In this step, `chosen` appears to be an iterable object that contains multiple `poem` objects. Each `poem` object likely contains a property named `graph`, which itself contains a property named `nodes`. Therefore, `chosen` is likely an array or another iterable object that holds multiple `poem` objects.\n\nThe structure of `chosen` seems to be something like this:\n\n```javascript\nconst chosen = [\n    {\n        graph: {\n            nodes: [\n                // Node objects\n            ]\n        }\n    },\n    // More poem objects\n];\n```\n\nBased on this deduction, `chosen` is likely an array of objects, where each object represents a `poem` with a `graph` property containing an array of `nodes`."
    },
    {
      "type": "pagefold",
      "id": "dd2b779bc4a0addd",
      "text": "~"
    },
    {
      "type": "reference",
      "id": "27f9eed35aee3547",
      "site": "scad.fed.wiki",
      "slug": "making-limited-edition-prints",
      "title": "Making Limited Edition Prints",
      "text": "I (Ward) developed what I now think of as a [[Poem]] written in a dialect somewhere between a proof and a tweetstorm, technically an algorithm. I collected snapshots as I worked. See [[Limited Edition Prints]]"
    }
  ],
  "journal": [
    {
      "type": "create",
      "item": {
        "title": "Derive Type of Chosen Variable",
        "story": []
      },
      "date": 1712669577203
    },
    {
      "item": {
        "type": "factory",
        "id": "8b4d6c1334d7a5e7"
      },
      "id": "8b4d6c1334d7a5e7",
      "type": "add",
      "date": 1712669612567
    },
    {
      "type": "edit",
      "id": "8b4d6c1334d7a5e7",
      "item": {
        "type": "markdown",
        "id": "8b4d6c1334d7a5e7",
        "text": "Based on Step 1, we can deduce the type of the `chosen` variable:\n\n```javascript\nfor (const poem of chosen) {\n    for (const node of poem.graph.nodes) {\n        vocabulary.add(hash(node));\n    }\n}\n```\n\nIn this step, `chosen` appears to be an iterable object that contains multiple `poem` objects. Each `poem` object likely contains a property named `graph`, which itself contains a property named `nodes`. Therefore, `chosen` is likely an array or another iterable object that holds multiple `poem` objects.\n\nThe structure of `chosen` seems to be something like this:\n\n```javascript\nconst chosen = [\n    {\n        graph: {\n            nodes: [\n                // Node objects\n            ]\n        }\n    },\n    // More poem objects\n];\n```\n\nBased on this deduction, `chosen` is likely an array of objects, where each object represents a `poem` with a `graph` property containing an array of `nodes`."
      },
      "date": 1712669615904
    },
    {
      "type": "edit",
      "id": "8b4d6c1334d7a5e7",
      "item": {
        "type": "markdown",
        "id": "8b4d6c1334d7a5e7",
        "text": "Based on the [[recommend]] function, we can deduce the type of the `chosen` variable:\n\n```javascript\nfor (const poem of chosen) {\n    for (const node of poem.graph.nodes) {\n        vocabulary.add(hash(node));\n    }\n}\n```\n\nIn this step, `chosen` appears to be an iterable object that contains multiple `poem` objects. Each `poem` object likely contains a property named `graph`, which itself contains a property named `nodes`. Therefore, `chosen` is likely an array or another iterable object that holds multiple `poem` objects.\n\nThe structure of `chosen` seems to be something like this:\n\n```javascript\nconst chosen = [\n    {\n        graph: {\n            nodes: [\n                // Node objects\n            ]\n        }\n    },\n    // More poem objects\n];\n```\n\nBased on this deduction, `chosen` is likely an array of objects, where each object represents a `poem` with a `graph` property containing an array of `nodes`."
      },
      "date": 1712669634641
    },
    {
      "item": {
        "type": "factory",
        "id": "12165aba1e6bfd6d"
      },
      "id": "12165aba1e6bfd6d",
      "type": "add",
      "after": "8b4d6c1334d7a5e7",
      "date": 1712669647414
    },
    {
      "id": "12165aba1e6bfd6d",
      "type": "move",
      "order": [
        "12165aba1e6bfd6d",
        "8b4d6c1334d7a5e7"
      ],
      "date": 1712669651534
    },
    {
      "type": "edit",
      "id": "8b4d6c1334d7a5e7",
      "item": {
        "type": "markdown",
        "id": "8b4d6c1334d7a5e7",
        "text": "Based on the [[recommend]] function, we can deduce the type of the `chosen` variable:"
      },
      "date": 1712669656136
    },
    {
      "type": "add",
      "id": "83e7a6b3bcc07e51",
      "item": {
        "type": "markdown",
        "id": "83e7a6b3bcc07e51",
        "text": "```javascript\nfor (const poem of chosen) {\n    for (const node of poem.graph.nodes) {\n        vocabulary.add(hash(node));\n    }\n}\n```\n\nIn this step, `chosen` appears to be an iterable object that contains multiple `poem` objects. Each `poem` object likely contains a property named `graph`, which itself contains a property named `nodes`. Therefore, `chosen` is likely an array or another iterable object that holds multiple `poem` objects.\n\nThe structure of `chosen` seems to be something like this:\n\n```javascript\nconst chosen = [\n    {\n        graph: {\n            nodes: [\n                // Node objects\n            ]\n        }\n    },\n    // More poem objects\n];\n```\n\nBased on this deduction, `chosen` is likely an array of objects, where each object represents a `poem` with a `graph` property containing an array of `nodes`."
      },
      "after": "8b4d6c1334d7a5e7",
      "date": 1712669658831
    },
    {
      "id": "8b4d6c1334d7a5e7",
      "type": "move",
      "order": [
        "8b4d6c1334d7a5e7",
        "12165aba1e6bfd6d",
        "83e7a6b3bcc07e51"
      ],
      "date": 1712669660541
    },
    {
      "type": "edit",
      "id": "83e7a6b3bcc07e51",
      "item": {
        "type": "markdown",
        "id": "83e7a6b3bcc07e51",
        "text": "\n```\n\nIn this step, `chosen` appears to be an iterable object that contains multiple `poem` objects. Each `poem` object likely contains a property named `graph`, which itself contains a property named `nodes`. Therefore, `chosen` is likely an array or another iterable object that holds multiple `poem` objects.\n\nThe structure of `chosen` seems to be something like this:\n\n```javascript\nconst chosen = [\n    {\n        graph: {\n            nodes: [\n                // Node objects\n            ]\n        }\n    },\n    // More poem objects\n];\n```\n\nBased on this deduction, `chosen` is likely an array of objects, where each object represents a `poem` with a `graph` property containing an array of `nodes`."
      },
      "date": 1712669669528
    },
    {
      "type": "edit",
      "id": "12165aba1e6bfd6d",
      "item": {
        "type": "code",
        "id": "12165aba1e6bfd6d",
        "text": "for (const poem of chosen) {\n    for (const node of poem.graph.nodes) {\n        vocabulary.add(hash(node));\n    }\n}"
      },
      "date": 1712669676110
    },
    {
      "type": "edit",
      "id": "83e7a6b3bcc07e51",
      "item": {
        "type": "markdown",
        "id": "83e7a6b3bcc07e51",
        "text": "In this step, `chosen` appears to be an iterable object that contains multiple `poem` objects. Each `poem` object likely contains a property named `graph`, which itself contains a property named `nodes`. Therefore, `chosen` is likely an array or another iterable object that holds multiple `poem` objects.\n\nThe structure of `chosen` seems to be something like this:\n\n```javascript\nconst chosen = [\n    {\n        graph: {\n            nodes: [\n                // Node objects\n            ]\n        }\n    },\n    // More poem objects\n];\n```\n\nBased on this deduction, `chosen` is likely an array of objects, where each object represents a `poem` with a `graph` property containing an array of `nodes`."
      },
      "date": 1712669684608
    },
    {
      "type": "edit",
      "id": "8b4d6c1334d7a5e7",
      "item": {
        "type": "markdown",
        "id": "8b4d6c1334d7a5e7",
        "text": "Based on the [[recommend]] function, we can deduce the type of the `chosen` variable: (ChatGPT 3.5)"
      },
      "date": 1712669712254
    },
    {
      "item": {
        "type": "factory",
        "id": "dd2b779bc4a0addd"
      },
      "id": "dd2b779bc4a0addd",
      "type": "add",
      "after": "83e7a6b3bcc07e51",
      "date": 1712669924388
    },
    {
      "type": "edit",
      "id": "dd2b779bc4a0addd",
      "item": {
        "type": "pagefold",
        "id": "dd2b779bc4a0addd",
        "text": "~"
      },
      "date": 1712669929664
    },
    {
      "item": {
        "type": "factory",
        "id": "27f9eed35aee3547"
      },
      "id": "27f9eed35aee3547",
      "type": "add",
      "after": "dd2b779bc4a0addd",
      "date": 1712669932251
    },
    {
      "type": "edit",
      "id": "27f9eed35aee3547",
      "item": {
        "type": "reference",
        "id": "27f9eed35aee3547",
        "site": "scad.fed.wiki",
        "slug": "making-limited-edition-prints",
        "title": "Making Limited Edition Prints",
        "text": "I developed what I now think of as a poem written in a dialect somewhere between a proof and a tweetstorm, technically an algorithm. I collected snapshots as I worked. See [[Limited Edition Prints]]"
      },
      "date": 1712669935362
    },
    {
      "type": "edit",
      "id": "27f9eed35aee3547",
      "item": {
        "type": "reference",
        "id": "27f9eed35aee3547",
        "site": "scad.fed.wiki",
        "slug": "making-limited-edition-prints",
        "title": "Making Limited Edition Prints",
        "text": "I (Ward) developed what I now think of as a poem written in a dialect somewhere between a proof and a tweetstorm, technically an algorithm. I collected snapshots as I worked. See [[Limited Edition Prints]]"
      },
      "date": 1712669961036
    },
    {
      "type": "edit",
      "id": "27f9eed35aee3547",
      "item": {
        "type": "reference",
        "id": "27f9eed35aee3547",
        "site": "scad.fed.wiki",
        "slug": "making-limited-edition-prints",
        "title": "Making Limited Edition Prints",
        "text": "I (Ward) developed what I now think of as a [[Poem]] written in a dialect somewhere between a proof and a tweetstorm, technically an algorithm. I collected snapshots as I worked. See [[Limited Edition Prints]]"
      },
      "date": 1712669985306
    },
    {
      "type": "fork",
      "site": "localhost:3000",
      "date": 1712670390637
    }
  ]
}