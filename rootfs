{
  "title": "Rootfs",
  "story": [
    {
      "type": "paragraph",
      "id": "d86ceb02b9c4198f",
      "text": "is a special instance of ramfs (or tmpfs, if that's enabled), which is always present in 2.6 systems. You can't unmount rootfs for approximately the same reason you can't kill the init process; rather than having special code to check for and handle an empty list, it's smaller and simpler for the kernel to just make sure certain lists can't become empty. [https://www.kernel.org/doc/html/latest/filesystems/ramfs-rootfs-initramfs.html page] (2005), [https://www.baeldung.com/linux/rootfs page]"
    }
  ],
  "journal": [
    {
      "type": "create",
      "item": {
        "title": "Rootfs",
        "story": []
      },
      "date": 1691236434660
    },
    {
      "item": {
        "type": "factory",
        "id": "d86ceb02b9c4198f"
      },
      "id": "d86ceb02b9c4198f",
      "type": "add",
      "date": 1691236436177
    },
    {
      "type": "edit",
      "id": "d86ceb02b9c4198f",
      "item": {
        "type": "paragraph",
        "id": "d86ceb02b9c4198f",
        "text": "s a special instance of ramfs (or tmpfs, if that's enabled), which is always present in 2.6 systems. You can't unmount rootfs for approximately the same reason you can't kill the init process; rather than having special code to check for and handle an empty list, it's smaller and simpler for the kernel to just make sure certain lists can't become empty."
      },
      "date": 1691236438161
    },
    {
      "type": "edit",
      "id": "d86ceb02b9c4198f",
      "item": {
        "type": "paragraph",
        "id": "d86ceb02b9c4198f",
        "text": "is a special instance of ramfs (or tmpfs, if that's enabled), which is always present in 2.6 systems. You can't unmount rootfs for approximately the same reason you can't kill the init process; rather than having special code to check for and handle an empty list, it's smaller and simpler for the kernel to just make sure certain lists can't become empty. [https://www.kernel.org/doc/html/latest/filesystems/ramfs-rootfs-initramfs.html kernel.org]"
      },
      "date": 1691236478373
    },
    {
      "type": "edit",
      "id": "d86ceb02b9c4198f",
      "item": {
        "type": "paragraph",
        "id": "d86ceb02b9c4198f",
        "text": "is a special instance of ramfs (or tmpfs, if that's enabled), which is always present in 2.6 systems. You can't unmount rootfs for approximately the same reason you can't kill the init process; rather than having special code to check for and handle an empty list, it's smaller and simpler for the kernel to just make sure certain lists can't become empty. [https://www.kernel.org/doc/html/latest/filesystems/ramfs-rootfs-initramfs.html page]"
      },
      "date": 1691236484525
    },
    {
      "type": "edit",
      "id": "d86ceb02b9c4198f",
      "item": {
        "type": "paragraph",
        "id": "d86ceb02b9c4198f",
        "text": "is a special instance of ramfs (or tmpfs, if that's enabled), which is always present in 2.6 systems. You can't unmount rootfs for approximately the same reason you can't kill the init process; rather than having special code to check for and handle an empty list, it's smaller and simpler for the kernel to just make sure certain lists can't become empty. [https://www.kernel.org/doc/html/latest/filesystems/ramfs-rootfs-initramfs.html page] (2005)"
      },
      "date": 1691236498118
    },
    {
      "type": "edit",
      "id": "d86ceb02b9c4198f",
      "item": {
        "type": "paragraph",
        "id": "d86ceb02b9c4198f",
        "text": "is a special instance of ramfs (or tmpfs, if that's enabled), which is always present in 2.6 systems. You can't unmount rootfs for approximately the same reason you can't kill the init process; rather than having special code to check for and handle an empty list, it's smaller and simpler for the kernel to just make sure certain lists can't become empty. [https://www.kernel.org/doc/html/latest/filesystems/ramfs-rootfs-initramfs.html page] (2005), [https://www.baeldung.com/linux/rootfs page]"
      },
      "date": 1691236547923
    }
  ]
}