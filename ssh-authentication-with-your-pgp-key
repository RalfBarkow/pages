{
  "title": "SSH Authentication with Your PGP Key",
  "story": [
    {
      "type": "markdown",
      "id": "675bb2c1a3d535e7",
      "text": "https://budts.be/weblog/2012/08/ssh-authentication-with-your-pgp-key/"
    },
    {
      "type": "markdown",
      "id": "75e12d871f63dc7f",
      "text": "GnuPG 2.0\n\nWhen you prefer to use the current stable version of GnuPG it takes a few additional steps. The problem is that your key is not available to `gpg-agent` to be used for SSH-authentication, so you need to get it in the `gpg-agent`. \n"
    },
    {
      "type": "markdown",
      "id": "fb421fc1f79f0766",
      "text": "One method to do this is by installing [https://packages.debian.org/bullseye/monkeysphere monkeysphere]. After installing this, you can run the command `monkeysphere subkey-to-ssh-agent` (or it's short form: `monkeysphere s`). Monkeysphere will then extract your private authentication key from GnuPG, run it through the `openpgp2ssh-script` to convert it into the correct format, and then add it to `gpg-agent` using the standard `ssh-add`-command. After you have ran that command your authentication subkey will (also) be stored by `gpg-agent` and it will be added to the `~/.gnupg/sshcontrol`-file so you can use it for SSH-authentication. (As with GnuPG 2.1, make sure that your gpg-agent is started with the --enable-ssh-support flag.)"
    }
  ],
  "journal": [
    {
      "type": "create",
      "item": {
        "title": "SSH Authentication with Your PGP Key",
        "story": []
      },
      "date": 1630595167216
    },
    {
      "id": "675bb2c1a3d535e7",
      "type": "add",
      "item": {
        "type": "markdown",
        "id": "675bb2c1a3d535e7",
        "text": "https://budts.be/weblog/2012/08/ssh-authentication-with-your-pgp-key/"
      },
      "date": 1630595184094
    },
    {
      "id": "75e12d871f63dc7f",
      "type": "add",
      "item": {
        "type": "markdown",
        "id": "75e12d871f63dc7f",
        "text": "GnuPG 2.0\n\nWhen you prefer to use the current stable version of GnuPG it takes a few additional steps. The problem is that your key is not available to `gpg-agent` to be used for SSH-authentication, so you need to get it in the `gpg-agent`. \n"
      },
      "after": "675bb2c1a3d535e7",
      "date": 1630595186198
    },
    {
      "id": "fb421fc1f79f0766",
      "type": "add",
      "item": {
        "type": "markdown",
        "id": "fb421fc1f79f0766",
        "text": "One method to do this is by installing monkeysphere (available from the ubuntu repos). After installing this, you can run the command `monkeysphere subkey-to-ssh-agent` (or it's short form: `monkeysphere s`). Monkeysphere will then extract your private authentication key from GnuPG, run it through the `openpgp2ssh-script` to convert it into the correct format, and then add it to `gpg-agent` using the standard `ssh-add`-command. After you have ran that command your authentication subkey will (also) be stored by `gpg-agent` and it will be added to the `~/.gnupg/sshcontrol`-file so you can use it for SSH-authentication. (As with GnuPG 2.1, make sure that your gpg-agent is started with the --enable-ssh-support flag.)"
      },
      "after": "75e12d871f63dc7f",
      "date": 1630595188226
    },
    {
      "type": "edit",
      "id": "fb421fc1f79f0766",
      "item": {
        "type": "markdown",
        "id": "fb421fc1f79f0766",
        "text": "One method to do this is by installing [https://packages.debian.org/bullseye/monkeysphere monkeysphere]. After installing this, you can run the command `monkeysphere subkey-to-ssh-agent` (or it's short form: `monkeysphere s`). Monkeysphere will then extract your private authentication key from GnuPG, run it through the `openpgp2ssh-script` to convert it into the correct format, and then add it to `gpg-agent` using the standard `ssh-add`-command. After you have ran that command your authentication subkey will (also) be stored by `gpg-agent` and it will be added to the `~/.gnupg/sshcontrol`-file so you can use it for SSH-authentication. (As with GnuPG 2.1, make sure that your gpg-agent is started with the --enable-ssh-support flag.)"
      },
      "date": 1630595345477
    }
  ]
}